repos:
  - repo: local
    hooks:
      - id: fix
        name: fix
        entry: uv run --locked just fix
        language: unsupported
        pass_filenames: false
      - id: typecheck
        name: typecheck
        entry: uv run --locked just typecheck
        language: unsupported
        pass_filenames: false
